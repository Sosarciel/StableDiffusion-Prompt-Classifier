# DiT 语义分割测试提示词转换规范 (Prompt Conversion Skill v2.0)

本规范专为 **DiT 架构模型（如 Flux 等搭载的 T5 编码器）**的多角色、多变体语义分割测试而设计。通过**高流动性、直白、硬核的纯英文自然语言**锁死视觉事实，防止潜空间色彩与特征污染，同时拦截破坏双人分割画布的一切外界干扰因素。

---

## 🛑 三大铁律与全面拦截网 (Core Constraints & Interception Guardrail)

1. **零主观修饰（Hard Facts Only）：** 
   彻底剔除所有类似 *striking, beautiful, elegant, breathtaking, glamorous* 的高频脑补词。它们不仅对 DiT 毫无用处，还会严重稀释关键特征的权重。只留客观的视觉事实（颜色、材质、具体对象）。

2. **拒绝机械式词条拼接（Anti-Tag-Gluing）：**
   坚决避免写出形如 `word, a word, word, and a word` 的无效伪句。禁止将 Tag 用简单的连词死板地串联。必须使用真正的流式英语逻辑，将形容词自然、合理地整合在名词前（例如：不说 *a dress with frills, with a bow, in black*，而是说 *a frilled black dress featuring a bow*）。

3. **全维度拦截机制（Comprehensive Interception Guardrail）：**
   在转换前，必须彻底审查原 Tag 流。**一旦发现以下任一类型的非静态服饰/人体特征词，必须在转换前向用户发出明确的【警告提示】，并在最终生成的自然语言中将其剥离过滤**，以防污染双角色构图测试：
   * **姿势与动作词：** 如 `wariza`, `sitting`, `standing`, `lying`, `hand on headwear`, `arm support` 等。
   * **手持物体词：** 如 `holding sword`, `holding umbrella`, `holding cup` 等任何角色手中拿取的道具。
   * **非氛围性背景：** 排除具体的具象地点（如 `classroom`, `street`, `indoor` 等），背景仅允许转换为无干涉的氛围性描述或独立成段。
   * **构图与镜头术语：** 如 `close up`, `upper body`, `cowboy shot`, `portrait` 等控制景别与镜头的词条。

---

## 📐 四段式标准结构蓝图 (The 4-Tier Structure)

转换后的提示词必须严格按照以下信息优先级进行分段，凡是新开段落，句首的 `She/Her` 必须强制换行，以保证清晰的语义边界。

### 1️⃣ 第一段：直观基础特征 (Base Profile)
* **固定句式：** `The girl [触发词1] [触发词2] is/has...`（**特别注意：The girl 后面绝对禁止加逗号**）。
* **包含内容：** 合并最直观的一眼可见特征，包括胸部大小、基础眼色、以及基础面部细节（如：发丝落于双眼间）。**绝对不允许拆成多句话描述。**

### 2️⃣ 第二段：特异性人体/发瞳细节 (Advanced Anatomy)
* **触发条件：** 仅在出现非典型发色（多色染/渐变/挑染）、特殊发型（双马尾/辫子组合）、特异瞳孔（符号瞳/星形瞳/异色瞳）或非人类器官（长耳朵/角）时独立开段。
* **换行规则：** 必须强制换行，并以 `She/Her` 开头。

### 3️⃣ 第三段：流式服装与配饰 (Outfit Setup)
* **包含内容：** 拒绝将配饰、鞋袜、衣服拆成碎片化的短句。整套服装的款式、材质、手套、腿部、鞋袜以及附带的项圈珠宝必须在一气呵成的流式长句内交代完毕，避免过度换行破坏服装整体感。
* **换行规则：** 必须强制换行，并以 `She/Her` 开头。

### 4️⃣ 第四段：特殊人设与环境氛围 (Special Lore & Atmosphere)
* **包含内容：** 
  * 针对无法直接被经典 Tag 机制解析的非人形非人类特征（如一蓬绿色荆棘多尾巴等）进行重磅、极端的数量枚举与具象化视觉隐喻翻译。
  * 剥离自角色身上的纯氛围性背景环境元素（如：围绕在四周、非长在身上的鲜花、绿叶、藤蔓气氛组）。
* **换行规则：** 必须强制换行独立成段，并以 `She/Her` 或环境主语开头。